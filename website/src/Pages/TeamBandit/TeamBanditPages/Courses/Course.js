
//// NOT BEING USED
import { Fragment, React } from "react";

//import CourseCard from "./CourseCard";
//import NewCourseTable from "./NewCourseTable";

const Course = ({courseInfo, courseChange}) => {
    
    return (
        <Fragment>
            <div>
            {/*courseInfo.course_title !== null && <NewCourseTable courseInfo={courseInfo} setCoursesChange={courseChange}/>*/}
            {/*courseInfo.course_title !== null && <CourseCard courseInfo={courseInfo} setCoursesChange={courseChange}/>*/}
            </div>
        </Fragment>
    );
};

export default Course;
