import { Button } from "@mui/material";

// TODO: Make button valid activation upon selection of a client field
// via click

// Button for removing an existing client from the Clients table
const RemoveClientButton = () => {
    return (
        <div>

            <Button variant= 'outlined'>
                Remove Client
            </Button>

        </div>
    );
}

export default RemoveClientButton;