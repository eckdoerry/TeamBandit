import { Button } from "@mui/material";

// TODO: Update to act as edit buttons for individual fields/cells
// rather than client rows altogether, just using this as placeholder for
// table functionality testing first.

// Button for editing a client's details in the Clients table
const EditClientButton = () => {
    return (
        <div>

            <Button variant= 'outlined'>
                Edit Client Details
            </Button>

        </div>
    );
}

export default EditClientButton;